using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpecialManager : MonoBehaviour
{
/*    public UpgradeUIManager uiUpgradeManager;
    public int countSpecial;



    void Start()
    {
        countSpecial = DatabaseManager.instance.countMainanInGame;

        for (int i = 0; i < countSpecial; i++)
        {
            if (i < SaveManager.instance.GetLevelShelf())
            {
                uiUpgradeManager.SetUpSpecialUI(i, true);
            }
            else
            {
                uiUpgradeManager.SetUpSpecialUI(i, false);
            }
        }
        
    }*/

}
